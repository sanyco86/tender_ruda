  <%= form_for [:admin, @tender.becomes(Tender)] do |f| %>
      <%= f.hidden_field :status_id, :value => 4 %>
      <%= f.submit 'Перенести тендер а архивные', class: 'btn btn-success btn-large' %></br></br>
  <% end %>
